<p>
  I&#8217;m a freelance Ruby/Rails engineer,
  who loves working with <strong>great people</strong>
  to make <strong>interesting things</strong>.
  If you’re creating something interesting,
  get in touch! You can probably
  <a href="/hire">hire me</a>.
</p>

<p>As my son once said:</p>

<blockquote>Let&#8217;s go build all the things!</blockquote>


<div class="row">
  <div class="col-md-12">
    <h4>Featured Posts</h4>
    <% featured_articles.each do |article| %>
      <a href="<%= article.url %>">
        <h5><%= article.title %></h5>
      </a>
      <blockquote>
        <%= article.data[:summary] %>
      </blockquote>
    <% end %>

    <small>
      <p><%= link_to 'More posts', '/blog/archives/' %></p>
    </small>
  </div>
</div>
